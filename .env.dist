# Copy this file to .env and adjust the values you need.

# Select which media server to deploy. Supported values: plex, jellyfin
# This controls the buzz update hook target and should match COMPOSE_PROFILES.
MEDIA_SERVER=$COMPOSE_PROFILES

# Path inside the media server container that matches the rclone mount.
LIBRARY_MOUNT=/mnt/buzz

# Local user/group IDs for services that should create host-owned files.
PUID=1000
PGID=1000

# Plex update hook settings.
# PLEX_URL should point to your Plex server, for example http://127.0.0.1:32400
PLEX_URL=
# Get this from Plex localStorage: window.localStorage.getItem("myPlexAccessToken")
PLEX_TOKEN=
# Optional Plex container settings.
PLEX_VERSION=docker
PLEX_CLAIM=

# Jellyfin update hook settings.
# JELLYFIN_URL must be reachable from the buzz container, for example http://jellyfin:8096
JELLYFIN_URL=http://jellyfin:8096
JELLYFIN_API_KEY=
# Optional: set this if your Jellyfin task name differs or auto-discovery fails.
JELLYFIN_SCAN_TASK_ID=

# Jellyfin curator settings. Override only if necessary
# CURATOR_BUILDER_URL=http://buzz-curator:8400/rebuild
# CURATOR_PORT=8400
# CURATOR_BUILD_ON_START=true
# CURATOR_SOURCE_ROOT=/mnt/buzz
# CURATOR_TARGET_ROOT=/mnt/jellyfin-library
# CURATOR_STATE_DIR=/app/data
# CURATOR_OVERRIDES=/config/overrides.yml

# Don't change this
COMPOSE_PROFILES=${MEDIA_SERVER}

# OpenSubtitles settings
SUBTITLE_ENABLED=false
OPENSUBTITLES_API_KEY=
OPENSUBTITLES_USERNAME=
OPENSUBTITLES_PASSWORD=
SUBTITLE_LANGUAGES=en
SUBTITLE_STRATEGY=most-downloaded
SUBTITLE_ROOT=/mnt/buzz/subs
