"""Deprecated helper placeholder for historical state.py refactors.

The previous contents were an incomplete one-off code generation script that
was not part of the application runtime and no longer reflects the current
`buzz/core/state.py` structure. The file remains as an explicit placeholder so
repo-wide type checks stay clean without pretending the old script is usable.
"""


def main() -> None:
    """Explain that this helper is intentionally retired."""
    print(
        "refactor_state.py is retired. Edit buzz/core/state.py directly or "
        "create a new focused migration script."
    )


if __name__ == "__main__":
    main()
